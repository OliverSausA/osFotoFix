<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="osFotoFix.Views.ImageView">
  <Border
    Background="Yellow"
    BorderBrush="Black"
    BorderThickness="2"
    CornerRadius="10"
    Padding="4"
    >
    <Grid RowDefinitions="*,Auto">
      <TextBox
        Text="{Binding Foto.FileNameOld}"
        />
      <Image>
      </Image>
      <StackPanel 
        Grid.Row="1"
        Orientation="Horizontal"
        HorizontalAlignment="Center"
        >
        <Button Width="50" Height="30" Content="Z" Command="{Binding PrevImageCmd}" />
        <Button Width="50" Height="30" Content="V" Command="{Binding NextImageCmd}" />
        <Button Width="50" Height="30" Content="X" />
        <Button Width="50" Height="30" Content="O" />
      </StackPanel>
    </Grid>
  </Border>
</UserControl>
